import javax.swing.*;

public class Q12 {
    static void main(String[] args) {
        JFrame newWindow;
        newWindow = new JFrame();
        newWindow.setSize(300,200);
        newWindow.setLocation(100,50);
        newWindow.setTitle("My First Frame");
        newWindow.setVisible(true);
    }
}
